// #! /usr/bin/env node

// const fs = require('fs');
// const path = require('path');
// const { Readable } = require('stream');
// const process_cwd = process.cwd();
// const yaml_file = 'pnpm-workspaced.yaml';
// const current_path = process_cwd + '/' + yaml_file;
// const writer = fs.createWriteStream(current_path, {
//   encoding: 'utf-8',
// });
// const from_path = path.resolve(__dirname, 'pnpm-workspace.yaml');
// const fstat = fs.fstatSync(from_path)
// if (fstat.isFile()) {

// }

// console.log({ from_path });
// // fs.createReadStream(__dirname + '/pnpm-workspace.yaml').pipe(writer);
// // buffered.pipe(writer);
// //

console.log('mario');
